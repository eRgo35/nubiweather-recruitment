## Call the API yourself!

API provides to routes:

- `/api/current-weather`
- `/api/forecast-weather`

To call the desired route use `GET` request type. For `/api/forecast-weather` additionally provide amount of days i.e. `/api/forecast-weather?days=3`.
